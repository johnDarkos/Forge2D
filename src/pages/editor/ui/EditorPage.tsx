import { SpriteEditor } from '@/widgets/sprite-editor'

/** Страница редактора: компоновка рабочего экрана, без логики нарезки и экспорта. */
export function EditorPage() {
  return <SpriteEditor />
}
